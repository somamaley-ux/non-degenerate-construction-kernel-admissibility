# Downstream compatibility and theorem routing

Existing import paths and declaration signatures are retained. Use `import AASCKernelPaperClean` for the complete supported reference surface, `import KernelReference` for the reference mathematics, or a specific module for a smaller dependency.

## Current necessity route

For the mathematical content of Proposition 2.4, use `KernelReference.IncidenceNecessity`, `KernelReference.NecessityRealization`, and `KernelReference.ObjecthoodNecessity` together. The starting relation and positive witness supply the semantic construction; identity, retention, exact realization, and witnessed defects supply its scope. `ObjecthoodNecessity.objecthood_necessity_semantic` assembles this construction and the universal encoding criterion. The manuscript establishes how these original data and derived functions express actual identity-bearing incidence and the four kernel roles.

Do not treat a retained `DerivedKernelRoles` record by itself as this complete derivation. Its presence remains compatible with the older represented API; the current chain additionally proves the regime's agreement with independently specified original qualification and the candidate-realization criterion.

| Existing surface | Exact meaning retained | Current route |
|---|---|---|
| `IncidenceSystem.Nondegenerate` | Some carrier/query pair has no incident result. It does not assert positive incidence. | `Foundation.HasActualIncidence`, `HasFailureBoundary`, and `HasBothWitnesses` distinguish the requirements. The new necessity construction needs an actual positive witness, not an additional failed locus. |
| `TargetAdequacy.Regime.Nondegenerate` | Both a standing and a nonstanding use exist. | `HasSuccessfulUse`, `HasFailedUse`, and `legacy_regime_nondegenerate_iff_both` expose this meaning. `ObjecthoodNecessity.canonicalRegime` is constructed from original qualification without demanding both polarities. |
| `TargetAdequacy.Redescription` | Explicit endpoint, target, and verdict preservation data. | `DependentConservation` derives result agreement from independently identified bearer/query and separate original incidence warrants. |
| `Manuscript.ActIdentity` | The represented equivalence includes verdict agreement. | `Foundation.OriginalPerformance` identifies occurrence, arrangement, conditions, and target before verdict. `IncidenceNecessity` proves original-incidence retention and non-substitution. |
| `DerivedKernelRoles`, `targetAdequacy_forces_kernel_roles` | A represented role reconstruction for the supplied regime. | `ObjecthoodNecessity` constructs the regime from `J` and proves its standing/admission semantics. Combine it with the identity, retention, and exact-realization theorems for the current necessity chain. |
| `Foundation.ObservationWork.CompleteFor` | Required-answer coverage only. | `NecessityRealization.Exact`, `exists_exact_reader_iff`, and the omission/surplus theorems express complete preservation and reflection of original observations. |
| `KernelRoleOmission`, `no_self_faithful_counterexample` | Results about their explicit represented predicates and packages. | `NecessityRealization` gives universal fibre, defect, and erasure tests against independently specified work. A reconstructible field may disappear without deleting its function. A failed ordinary use is not deletion of a role. |
| `no_faithful_lower_generator` | Exclusion of its explicitly packaged basis/generation/fidelity data. | `Authorization.finite_authorization_cut` derives the interpreted cut. `ObjecthoodNecessity.finite_authorization_cut_has_original_kernel` extracts its original witness and constructs its kernel realization. |
| `no_intermediate_status` | A classical admitted-equivalent/not-admitted-equivalent split. | Canonical status, semantic conservation, and exact observer descent state the stronger results with their original premises. The raw API need not have only two arbitrary profiles. |
| Unary or pre-supplied tuple descent | Operation preservation under the declared congruence premises. | `ContextualComposition` derives finite heterogeneous tuple congruence from original one-hole tests and constructs the product-of-sort-quotients operation with its exact original domain. |
| `constructional_report_preservation` | Consequence of an explicit report-support premise. | `Authorization.report_from_local_evidence` derives support from complete ancestor coverage, actual local checks, and sound local rules. `ComparisonAndReindexing` derives support preservation from original edge and role-data preservation. |
| `main_fixed_domain_exhaustion` | The original represented assembly with a standing-preservation premise. | Follow the current theorem map for Theorem 7.1's necessity, universal realization, finite authorization, conservation, and representation components. |

## Application boundaries

Downstream applications establish the original subject, complete question, incidence or use condition, and relevant contexts before claiming preservation. Original semantic identity may quotient redundant coordinate or gauge descriptions. Identity probes apply to that original identity; adding them retrospectively to a task that claimed only coarser observations changes the task.

Qualification for some experimental outcome does not establish a chosen practical success criterion. Supply the latter's original witness relation, or its one-point predicate representation. A settled negative qualification excludes every original witness. A missing message or unfinished search supplies no such evidence.

No existing downstream conclusion changes merely because additional files are imported. Use the current route when claiming its stronger derivation and discharge the corresponding premises. Classification uniqueness does not imply occupant uniqueness; primitive semantic comparisons need a separate finite-zigzag completeness proof; finite congruence and authorization do not establish arbitrary infinite-use or liveness claims. Full carrier-permutation symmetry is not inferred from object identity alone.
